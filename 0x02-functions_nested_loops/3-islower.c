#include <stdio.h>
#include <stdlib.h>

/**
* _islower - main function
* @c: the number to print out
*
* Return: return value
*/

int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
